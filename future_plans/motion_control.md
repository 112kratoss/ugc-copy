# Motion Control

This feature-specific note has been consolidated into `future_plans/model_integration.md`.

Use the shared document for:
- the current motion model registry
- the real callback and polling behavior
- model id mapping between UI and provider
- onboarding steps for new motion models
- known implementation gaps
