# Video Generation

This feature-specific note has been consolidated into `future_plans/model_integration.md`.

Use the shared document for:
- the current video generation flow
- the single-model limitation in the checked-in UI
- polling and storage persistence behavior
- onboarding steps for future video models
- current architecture cleanup targets
